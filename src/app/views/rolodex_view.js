import Backbone from 'backbone';

const RolodexView = Backbone.View.extend({
  initialize: function() {
    this.render();
  },

  render: function() {
    // your code here
    return this;
  }
});

export default ApplicationView;
